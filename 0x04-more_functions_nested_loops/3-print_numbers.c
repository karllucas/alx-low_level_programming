#include "main.h"

/**
 * Function prints numbers from 0 to 9
 *
 * Return: Always 0.
*/

void print_numbers(void)
{
        int ch;

        for (ch = 48; ch < 58; ch++)
        {
                _putchar(ch);
		return(0);
	}
	_putchar('\n');
}
