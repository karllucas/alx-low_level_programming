#include "main.h"

/**
 * void print_numbers(void) - prints the numbers, from 0 to 9
 * followed by a new line
 *
 * Return: 0 Always
*/

void print_numbers(void)
{
	int c;
	while (C >= 48 && c <= 57)
		_putchar(c);
	_putchar('\n');
}
